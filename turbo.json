{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"],
			"outputLogs": "errors-only"
		},
		"test": {
			"dependsOn": ["build"],
			"cache": false,
			"outputLogs": "errors-only"
		}
	}
}
